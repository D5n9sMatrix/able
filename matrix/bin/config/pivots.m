#!/snap/bin/octave 

# numeric values of Array in arg type A
A = [1, 1, 2; 3, 5, 8; 13, 21, 34]

# numeric values of Array in arg type B
B = rand(3, 2)

# compare the what 3 big wait B
3 > B

# numeric values level equals to Array
# form the equation numeric the values
# relative the number
4 > 2 * A

# multiples Array in arg type values
5 > A * B

# form transpose values relative the type 
# of order numeric
6 > A * B

# arguments next clear handle text layout
# numeric values arithmetic the axes
# values Ax = B
x = A * B

# numeric order values number handle view
# about case lines the link of values in
# wait the factor Array ...
A1 = [2, 0; 2, 0];
b = [2; 1];
xx = A * B
 
# order numeric in values scientific
x0 = [1; 2];

# form numeric values xp preferences
# to order dir in values symvar
t = linspace(0, 50, 200)

# utils form of analysis usejava
# numeric values to surface
# magic
m = "f", x0, t
p = "f", x0, t
c = "f", x0, t
a = "f", x0, t
j = "f", x0, t
r = "f", x0, t
d = "f", x0, t



     