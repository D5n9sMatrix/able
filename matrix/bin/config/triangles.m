#!/snap/bin/octave 

# form util usage methods utilized triangles
lu = [0, 1, 2; 3, 8, 9; 5, 6, 4; 0, 0, 7];

# create form usage literal methods tringles
lu * 2 + pi
lu * 4 + pi
lu * 8 + pi
lu * 3 + pi

# logical form utilized to example batter
date = [2022]
# logical util info from beta
diary date
# alpha in triangles to delta matrix_type
A = 1
# brain type info memory
typeinfo(matrix_type(A))
# create brain one var the memory set
matrix = A * 2 + pi
# create brain values memory set
matrix * A + svd(2, 3)
# pack refresh of matrix
# by handle in develop
matrix * A + svd(2, 2)
# rescale the develop
classname = class(matrix) 
# double of matrix
matrix * double(lu)

