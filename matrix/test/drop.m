#!/snap/bin/octave 

# host the expression of exponential
H = exp(10)

# host equivalent numeric server cosd H
format long
H = cosd(H)

# used the form arithmetic in values
N = -1
 
# cos of N in exp number
N = cos(exp(N))

# expression values increements
U = log(H * pi + sind(N))

# values of expression geometric
U = log2(U ^ H * pi + sin(N))

